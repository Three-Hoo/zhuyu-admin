import { createPage } from '@/core/create-page'
import { {{ camelCase name }}MetaList } from '@/sources/{{ dashCase name }}'

export default createPage({
  api: '/api/{{ dashCase name }}',
  title: '{{ cn_name }}',
  columns: {{ camelCase name }}MetaList,
})
